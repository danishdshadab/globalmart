version: 2

models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: orderid
        description: unique key
      - name: shipmode
        description: '{{doc("shipmode")}}'
        